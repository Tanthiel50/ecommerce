<form method="post" action="{{ path('app_sales_delete', {'id': sale.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer cette promotion ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sale.id) }}">
    <button class="btn btn-delete mt-3">Supprimer</button>
</form>
