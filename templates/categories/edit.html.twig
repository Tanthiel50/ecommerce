{% extends 'admin/base.html.twig' %}

{% block title %}Edition de catégorie{% endblock %}

{% block body %}
    <h1>Editer {{category.title}}</h1>

    {{ include('categories/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

    <a href="{{ path('app_categories_index') }}" class="btn btn-edit">Retour à la liste de gammes</a>

    {{ include('categories/_delete_form.html.twig') }}
{% endblock %}
